package ${templateInfo.packageName};

import com.cimr.comm.base.IotBaseService;
import com.${templateInfo.companyName}.${templateInfo.projectName}.model.${tableModel.domainName};
/**
 * ${tableModel.tableChineseName}的相关业务处理接口
 * @author ${author} ${tableModel.currentDate} ${tableModel.currentTime}
 */
public interface ${tableModel.domainName}Service extends IotBaseService<${tableModel.domainName},String> {
    
	//TODO 在此添加其他业务处理的方法
}